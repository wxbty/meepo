<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--命名空间应该是对应接口的包名+接口名 -->
<mapper namespace="com.bytesvc.service.mapper.AccountDao">
    <!--id应该是接口中的方法，结果类型如没有配置别名则应该使用全名称 -->
    <!--获得所有图书 -->
    <select id="getAll" resultType="com.bytesvc.service.pojo.Account">
        select acct_id,amount from tb_account_one
    </select>
    <!--获得图书对象通过编号 -->
    <!--<select id="getBookById" resultType="Book">-->
        <!--select id,title,price,publishDate from books where id=#{id}-->
    <!--</select>-->
    <!-- 增加 -->
    <insert id="add" parameterType="com.bytesvc.service.pojo.Account">
        insert into tb_account_one(acct_id,amount)
        values(#{acctId},#{amount})
    </insert>
    <!-- 删除 -->
    <delete id="delete">
        delete from books where id=#{id}
    </delete>
    <!-- 更新 -->
    <update id="update">
        update books set title=#{title},price=#{price},publishDate=#{publishDate}
        where id=#{id}
    </update>
</mapper>